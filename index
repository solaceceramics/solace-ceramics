<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Solace Ceramics — Grief Kintsugi</title>
<meta name="description" content="Hand-thrown ceramic pieces,(Or other statement.)." />
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght,SOFT@0,9..144,300..600,30..100;1,9..144,300..600,30..100&family=Newsreader:ital,opsz,wght@0,6..72,300..500;1,6..72,300..500&display=swap" rel="stylesheet">
<style>
  /* -------- TOKENS -------- */
  :root {
    --bone: #F2EBDC;
    --bone-2: #ECE3D0;
    --bone-3: #E5DAC2;
    --charcoal: #2A2622;
    --charcoal-soft: #4A413A;
    --charcoal-faint: rgba(42,38,34,.78);
    --gold: #B68B4C;
    --gold-deep: #8A6730;
    --rule: rgba(42,38,34,.18);
    --maxw: 1180px;
    --readw: 640px;
    --serif-display: "Fraunces", Georgia, serif;
    --serif-body: "Newsreader", "Iowan Old Style", Georgia, serif;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body {
    font-family: var(--serif-body);
    font-weight: 430;
    font-size: 18px;
    line-height: 1.65;
    color: var(--charcoal);
    background: var(--bone);
    -webkit-font-smoothing: antialiased;
    text-rendering: optimizeLegibility;
    overflow-x: hidden;
  }

  /* Subtle paper grain over the whole site */
  body::before {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 1;
    opacity: .35;
    mix-blend-mode: multiply;
    background-image: url("data:image/svg+xml;utf8,<svg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/><feColorMatrix values='0 0 0 0 0.16 0 0 0 0 0.15 0 0 0 0 0.13 0 0 0 0.45 0'/></filter><rect width='100%' height='100%' filter='url(%23n)'/></svg>");
  }

  ::selection { background: var(--gold); color: var(--bone); }

  /* Warm scrollbar */
  ::-webkit-scrollbar { width: 10px; height: 10px; }
  ::-webkit-scrollbar-track { background: var(--bone-2); }
  ::-webkit-scrollbar-thumb { background: var(--bone-3); border: 2px solid var(--bone-2); border-radius: 10px; }
  ::-webkit-scrollbar-thumb:hover { background: var(--gold); }

  /* Accessible focus rings */
  *:focus { outline: none; }
  *:focus-visible {
    outline: 2px solid var(--gold);
    outline-offset: 3px;
    border-radius: 1px;
  }
  input:focus-visible,
  textarea:focus-visible {
    outline: none;
  }

  a { color: inherit; text-decoration: none; }

  /* -------- NAV -------- */
  nav.top {
    position: fixed;
    top: 0; left: 0; right: 0;
    z-index: 50;
    padding: 22px 36px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: linear-gradient(to bottom, var(--bone) 60%, rgba(242,235,220,0));
    transition: padding .4s ease;
  }
  nav.top.scrolled { padding: 14px 36px; }
  nav.top .mark {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 350;
    font-size: 22px;
    letter-spacing: .01em;
    cursor: pointer;
  }
  nav.top .mark span { color: var(--gold); }
  nav.top ul {
    list-style: none;
    display: flex;
    gap: 36px;
    align-items: center;
  }
  nav.top a {
    font-family: var(--serif-body);
    font-size: 15px;
    font-weight: 400;
    color: var(--charcoal-soft);
    position: relative;
    padding-bottom: 4px;
    transition: color .3s ease;
  }
  nav.top a::after {
    content: "";
    position: absolute;
    left: 0; right: 100%; bottom: 0;
    height: 1px;
    background: var(--gold);
    transition: right .4s cubic-bezier(.22,1,.36,1);
  }
  nav.top a:hover { color: var(--charcoal); }
  nav.top a:hover::after { right: 0; }
  nav.top .menu-btn {
    display: none;
    background: none;
    border: 0;
    cursor: pointer;
    padding: 8px 4px;
    flex-direction: column;
    gap: 5px;
    width: 28px;
  }
  nav.top .menu-btn span {
    display: block;
    width: 100%;
    height: 1.5px;
    background: var(--charcoal);
    transition: transform .4s ease, opacity .3s ease;
  }
  nav.top .menu-btn.open span:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }
  nav.top .menu-btn.open span:nth-child(2) {
    opacity: 0;
  }
  nav.top .menu-btn.open span:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  /* Mobile menu overlay */
  .mobile-menu {
    position: fixed;
    inset: 0;
    background: var(--bone);
    z-index: 49;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 36px;
    opacity: 0;
    visibility: hidden;
    transition: opacity .5s ease, visibility .5s ease;
  }
  .mobile-menu.open {
    opacity: 1;
    visibility: visible;
  }
  .mobile-menu::before {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    opacity: .35;
    mix-blend-mode: multiply;
    background-image: url("data:image/svg+xml;utf8,<svg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/><feColorMatrix values='0 0 0 0 0.16 0 0 0 0 0.15 0 0 0 0 0.13 0 0 0 0.45 0'/></filter><rect width='100%' height='100%' filter='url(%23n)'/></svg>");
  }
  .mobile-menu a {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 320;
    font-size: 36px;
    color: var(--charcoal);
    position: relative;
    z-index: 1;
    transition: color .3s ease;
  }
  .mobile-menu a:hover {
    color: var(--gold-deep);
  }

  /* -------- LAYOUT -------- */
  section {
    position: relative;
    z-index: 2;
    padding: 140px 36px;
  }
  .container { max-width: var(--maxw); margin: 0 auto; }
  .read { max-width: var(--readw); margin: 0 auto; }

  /* -------- HERO -------- */
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 80px;
    padding-top: 160px;
    padding-bottom: 100px;
  }
  .hero-text .eyebrow {
    font-family: var(--serif-body);
    font-style: italic;
    font-size: 14px;
    color: var(--gold-deep);
    letter-spacing: .14em;
    text-transform: uppercase;
    margin-bottom: 28px;
    opacity: 0;
    animation: fadeUp 1.1s .1s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero h1 {
    font-family: var(--serif-display);
    font-weight: 320;
    font-size: clamp(54px, 7vw, 96px);
    line-height: .98;
    letter-spacing: -.025em;
    color: var(--charcoal);
    margin-bottom: 32px;
    opacity: 0;
    animation: fadeUp 1.2s .25s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero h1 em {
    font-style: italic;
    font-weight: 320;
    font-variation-settings: "SOFT" 100;
  }
  .hero h1 .gold-line {
    display: inline-block;
    position: relative;
  }
  .hero h1 .gold-line::after {
    content: "";
    position: absolute;
    left: 0; right: 0; bottom: .12em;
    height: 2px;
    background: var(--gold);
    transform: scaleX(0);
    transform-origin: left;
    animation: drawLine 2s 1.6s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero .sub {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 22px;
    line-height: 1.5;
    color: var(--charcoal-soft);
    margin-bottom: 24px;
    max-width: 460px;
    opacity: 0;
    animation: fadeUp 1.2s .55s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero .core-msg {
    font-size: 17px;
    font-style: italic;
    color: var(--charcoal-faint);
    max-width: 460px;
    margin-bottom: 40px;
    opacity: 0;
    animation: fadeUp 1.2s .8s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero .read-link {
    font-family: var(--serif-body);
    font-size: 16px;
    color: var(--charcoal);
    border-bottom: 1px solid var(--gold);
    padding-bottom: 4px;
    opacity: 0;
    animation: fadeUp 1.2s 1s cubic-bezier(.22,1,.36,1) forwards;
    transition: color .3s ease, letter-spacing .4s ease;
  }
  .hero .read-link:hover { letter-spacing: .04em; }

  .hero-image {
    aspect-ratio: 4 / 5;
    background: linear-gradient(160deg, var(--bone-3) 0%, var(--bone-2) 100%);
    position: relative;
    overflow: hidden;
    opacity: 0;
    animation: fadeUp 1.4s .4s cubic-bezier(.22,1,.36,1) forwards;
  }
  .hero-image::after {
    content: "image coming";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 17px;
    color: var(--charcoal-faint);
    letter-spacing: .04em;
  }
  /* a faint suggested gold line on the placeholder hero image */
  .hero-image::before {
    content: "";
    position: absolute;
    top: 18%; left: 8%;
    width: 84%; height: 1px;
    background: linear-gradient(to right, transparent 0%, var(--gold) 30%, var(--gold-deep) 50%, var(--gold) 70%, transparent 100%);
    transform: rotate(-22deg);
    transform-origin: left center;
    opacity: .5;
  }

  /* -------- ANIMATIONS -------- */
  @keyframes fadeUp {
    0%   { opacity: 0; transform: translateY(18px); }
    100% { opacity: 1; transform: translateY(0); }
  }
  @keyframes drawLine {
    0%   { transform: scaleX(0); }
    100% { transform: scaleX(1); }
  }
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition: opacity 1.2s cubic-bezier(.22,1,.36,1), transform 1.2s cubic-bezier(.22,1,.36,1);
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  /* -------- FILL-IN BLOCKS (placeholder prompts for mom) -------- */
  .fill-in {
    display: block;
    background: rgba(182,139,76,.07);
    border-left: 2px solid var(--gold);
    padding: 22px 26px;
    margin: 24px 0;
    font-family: var(--serif-body);
    font-style: italic;
    font-size: 16px;
    line-height: 1.65;
    color: var(--charcoal);
    text-align: left;
  }
  .fill-in::before {
    content: "Write here";
    display: block;
    font-family: var(--serif-body);
    font-style: normal;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: .22em;
    text-transform: uppercase;
    color: var(--gold-deep);
    margin-bottom: 10px;
  }
  .fill-in .hint {
    font-size: 14px;
    color: var(--charcoal-soft);
    display: block;
    margin-top: 10px;
    line-height: 1.55;
  }
  .fill-in.compact {
    padding: 16px 20px;
    margin: 12px 0;
  }
  .fill-in.compact::before {
    font-size: 10px;
    margin-bottom: 6px;
  }

  /* Placeholder h2/h3 — for headings that are awaiting words */
  .placeholder-h2 {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 320;
    font-size: clamp(36px, 4.4vw, 56px);
    line-height: 1.05;
    letter-spacing: -.015em;
    color: var(--gold-deep);
    margin-bottom: 32px;
    opacity: .68;
  }
  h3.placeholder {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 320;
    font-size: 18px;
    color: var(--gold-deep);
    opacity: .75;
    margin-bottom: 6px;
  }

  /* Add-piece tile — the empty slot at the end of a gallery row */
  .piece.add-piece {
    cursor: default;
  }
  .piece.add-piece:hover { transform: none; }
  .piece-image.add-piece-img {
    background: transparent;
    border: 1px dashed rgba(138,103,48,.28);
    transition: border-color .4s ease, background .4s ease;
  }
  .piece-image.add-piece-img::after,
  .piece-image.add-piece-img::before { display: none; }
  .piece-image.add-piece-img {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .piece.add-piece:hover .piece-image.add-piece-img {
    border-color: var(--gold);
    background: rgba(182,139,76,.04);
  }
  .piece.add-piece h3.placeholder {
    color: var(--charcoal-soft);
    opacity: .6;
  }
  .piece.add-piece .meta {
    font-size: 13px;
  }

  /* -------- SECTION HEADERS -------- */
  .eyebrow-small {
    font-family: var(--serif-body);
    font-style: italic;
    font-size: 13px;
    color: var(--gold-deep);
    letter-spacing: .18em;
    text-transform: uppercase;
    margin-bottom: 24px;
  }
  h2 {
    font-family: var(--serif-display);
    font-weight: 320;
    font-size: clamp(36px, 4.4vw, 56px);
    line-height: 1.05;
    letter-spacing: -.02em;
    color: var(--charcoal);
    margin-bottom: 32px;
  }
  h2 em {
    font-style: italic;
    font-variation-settings: "SOFT" 100;
  }

  /* -------- THE SHAPE OF IT -------- */
  .shape {
    background: var(--bone-2);
    text-align: center;
  }
  .shape .container { max-width: 760px; }
  .shape p {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: clamp(22px, 2.4vw, 28px);
    line-height: 1.55;
    color: var(--charcoal);
  }
  .shape .divider {
    width: 1px; height: 80px;
    background: var(--gold);
    margin: 0 auto 60px;
    opacity: .8;
  }

  /* -------- A FEW PIECES (PREVIEW) -------- */
  .preview .container {
    display: grid;
    grid-template-columns: 1fr;
    gap: 60px;
  }
  .preview-header {
    display: flex;
    justify-content: space-between;
    align-items: end;
    flex-wrap: wrap;
    gap: 20px;
  }
  .preview-header h2,
  .preview-header .placeholder-h2 { margin-bottom: 0; }
  .preview-link {
    font-size: 15px;
    border-bottom: 1px solid var(--gold);
    padding-bottom: 4px;
    color: var(--charcoal);
    transition: letter-spacing .4s ease;
  }
  .preview-link:hover { letter-spacing: .04em; }

  .pieces-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
  }
  .piece {
    cursor: pointer;
    transition: transform .8s cubic-bezier(.22,1,.36,1);
  }
  .piece:hover { transform: translateY(-4px); }
  .piece-image {
    aspect-ratio: 3 / 4;
    background: linear-gradient(160deg, var(--bone-3) 0%, var(--bone-2) 100%);
    position: relative;
    overflow: hidden;
    margin-bottom: 18px;
  }
  .piece-image::after {
    content: "image coming";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 15px;
    color: var(--charcoal-faint);
    letter-spacing: .04em;
  }
  .piece-image::before {
    content: "";
    position: absolute;
    top: var(--line-top, 35%);
    left: var(--line-left, 12%);
    width: var(--line-width, 70%);
    height: 1px;
    background: linear-gradient(to right, transparent, var(--gold) 40%, var(--gold-deep) 55%, var(--gold) 70%, transparent);
    transform: rotate(var(--line-angle, -15deg));
    opacity: .45;
  }
  .piece h3 {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 350;
    font-size: 20px;
    color: var(--charcoal);
    margin-bottom: 6px;
  }
  .piece .meta {
    font-size: 14px;
    color: var(--charcoal-faint);
    font-style: italic;
  }
  .piece .status {
    display: inline-block;
    margin-top: 10px;
    font-size: 12px;
    letter-spacing: .12em;
    text-transform: uppercase;
    color: var(--gold-deep);
  }
  .piece .status::before {
    content: "·";
    margin-right: 6px;
  }

  /* -------- HOW IT WORKS -------- */
  .how {
    background: var(--bone-2);
  }
  .how .container { max-width: 980px; text-align: center; }
  .how-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 60px;
    margin-top: 60px;
    margin-bottom: 80px;
  }
  .how-step .num {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 36px;
    color: var(--gold);
    margin-bottom: 16px;
  }
  .how-step p {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 22px;
    line-height: 1.4;
    color: var(--charcoal);
  }
  .button {
    display: inline-block;
    font-family: var(--serif-body);
    font-size: 16px;
    color: var(--charcoal);
    padding: 18px 40px;
    border: 1px solid var(--charcoal);
    background: transparent;
    cursor: pointer;
    transition: all .5s cubic-bezier(.22,1,.36,1);
    letter-spacing: .02em;
  }
  .button:hover {
    background: var(--charcoal);
    color: var(--bone);
    letter-spacing: .04em;
  }
  .button-gold {
    border-color: var(--gold-deep);
    color: var(--gold-deep);
  }
  .button-gold:hover {
    background: var(--gold-deep);
    color: var(--bone);
  }

  /* -------- THE STORY -------- */
  .story .read p {
    margin-bottom: 28px;
    font-size: 19px;
    line-height: 1.75;
    color: var(--charcoal);
  }
  .story .read p:first-of-type::first-letter {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 320;
    font-size: 78px;
    float: left;
    line-height: .85;
    margin: 8px 12px 0 0;
    color: var(--gold-deep);
  }
  .story .interstitial-image {
    aspect-ratio: 16 / 9;
    background: linear-gradient(160deg, var(--bone-3) 0%, var(--bone-2) 100%);
    margin: 60px 0;
    position: relative;
    overflow: hidden;
    max-width: 720px;
    margin-left: auto;
    margin-right: auto;
  }
  .story .interstitial-image::after {
    content: "image coming";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 15px;
    color: var(--charcoal-faint);
    letter-spacing: .04em;
  }
  .story .interstitial-image::before {
    content: "";
    position: absolute;
    top: var(--line-top, 40%);
    left: var(--line-left, 8%);
    width: var(--line-width, 80%);
    height: 1px;
    background: linear-gradient(to right, transparent, var(--gold) 40%, var(--gold-deep) 55%, var(--gold) 70%, transparent);
    transform: rotate(var(--line-angle, -10deg));
    opacity: .45;
  }
  .story .core-message {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: clamp(26px, 3vw, 34px);
    line-height: 1.45;
    text-align: center;
    color: var(--charcoal);
    margin-top: 80px;
    padding-top: 60px;
    border-top: 1px solid var(--rule);
  }

  /* -------- FULL GALLERY -------- */
  .gallery {
    background: var(--bone-2);
  }
  .gallery .intro {
    max-width: 600px;
    text-align: center;
    margin: 0 auto 80px;
    font-style: italic;
    font-family: var(--serif-display);
    font-weight: 300;
    font-size: 21px;
    line-height: 1.5;
    color: var(--charcoal-soft);
  }
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 50px;
    margin-bottom: 80px;
  }
  .gallery .center { text-align: center; }

  /* -------- A NOTE / CTA TAIL -------- */
  .tail {
    text-align: center;
    background: var(--bone);
    padding: 160px 36px;
  }
  .tail .core-message {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: clamp(28px, 3.4vw, 40px);
    line-height: 1.4;
    color: var(--charcoal);
    max-width: 720px;
    margin: 0 auto 50px;
  }

  /* -------- FOOTER -------- */
  footer {
    background: var(--charcoal);
    color: var(--bone);
    padding: 80px 36px 40px;
    position: relative;
    z-index: 2;
  }
  footer .container {
    display: flex;
    justify-content: space-between;
    align-items: end;
    flex-wrap: wrap;
    gap: 40px;
  }
  footer .mark {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 320;
    font-size: 28px;
  }
  footer .mark span { color: var(--gold); }
  footer .meta {
    font-family: var(--serif-body);
    font-style: italic;
    font-size: 14px;
    line-height: 1.7;
    color: rgba(242,235,220,.85);
    text-align: right;
  }
  footer .meta a {
    color: var(--bone);
    border-bottom: 1px solid var(--gold);
    padding-bottom: 2px;
  }
  footer .footer-tagline {
    color: rgba(242,235,220,.55);
    font-style: italic;
  }

  /* -------- COMMISSION FORM (full-screen modal) -------- */
  .modal {
    position: fixed;
    inset: 0;
    background: var(--bone);
    z-index: 100;
    opacity: 0;
    visibility: hidden;
    transition: opacity .6s ease, visibility .6s ease;
    overflow-y: auto;
  }
  .modal.open { opacity: 1; visibility: visible; }
  .modal::before {
    content: "";
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 1;
    opacity: .35;
    mix-blend-mode: multiply;
    background-image: url("data:image/svg+xml;utf8,<svg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'><filter id='n'><feTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='2' stitchTiles='stitch'/><feColorMatrix values='0 0 0 0 0.16 0 0 0 0 0.15 0 0 0 0 0.13 0 0 0 0.45 0'/></filter><rect width='100%' height='100%' filter='url(%23n)'/></svg>");
  }
  .modal-shell {
    position: relative;
    z-index: 2;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    opacity: 0;
    transform: translateY(20px);
    transition: opacity .6s cubic-bezier(.22,1,.36,1) .1s, transform .6s cubic-bezier(.22,1,.36,1) .1s;
  }
  .modal.open .modal-shell {
    opacity: 1;
    transform: translateY(0);
  }
  .modal-top {
    padding: 28px 36px;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .modal-top .label {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 350;
    font-size: 18px;
    color: var(--charcoal);
  }
  .modal-top .label span { color: var(--gold); }
  .close-btn {
    background: none;
    border: 0;
    font-family: var(--serif-body);
    font-size: 14px;
    color: var(--charcoal-soft);
    letter-spacing: .14em;
    text-transform: uppercase;
    cursor: pointer;
    padding-bottom: 4px;
    border-bottom: 1px solid transparent;
    transition: border-color .3s ease, color .3s ease;
  }
  .close-btn:hover {
    color: var(--charcoal);
    border-color: var(--gold);
  }

  .progress {
    display: flex;
    justify-content: center;
    gap: 16px;
    padding: 20px 0 50px;
  }
  .progress .dot {
    width: 24px;
    height: 2px;
    background: var(--rule);
    transition: background .5s ease, transform .5s ease;
  }
  .progress .dot.done { background: var(--charcoal-soft); }
  .progress .dot.current {
    background: var(--gold);
    transform: scaleY(2.5);
  }

  .step-wrap {
    flex: 1;
    display: flex;
    align-items: flex-start;
    justify-content: center;
    padding: 0 36px 80px;
  }
  .step {
    width: 100%;
    max-width: 920px;
    display: none;
    opacity: 0;
    transform: translateY(14px);
    transition: opacity .5s ease, transform .5s ease;
  }
  .step.active {
    display: block;
    opacity: 1;
    transform: translateY(0);
  }

  .step-eyebrow {
    font-family: var(--serif-body);
    font-style: italic;
    font-size: 13px;
    color: var(--gold-deep);
    letter-spacing: .18em;
    text-transform: uppercase;
    margin-bottom: 20px;
    text-align: center;
  }
  .step-question {
    font-family: var(--serif-display);
    font-weight: 320;
    font-size: clamp(32px, 3.6vw, 44px);
    line-height: 1.1;
    letter-spacing: -.02em;
    color: var(--charcoal);
    margin-bottom: 18px;
    text-align: center;
  }
  .step-question em {
    font-style: italic;
    font-variation-settings: "SOFT" 100;
  }
  .step-instruction {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 18px;
    color: var(--charcoal-soft);
    text-align: center;
    margin-bottom: 56px;
    max-width: 540px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Option cards */
  .options-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
    margin-bottom: 40px;
  }
  .option-card {
    background: var(--bone-2);
    border: 1px solid transparent;
    padding: 0;
    cursor: pointer;
    text-align: left;
    font-family: inherit;
    color: inherit;
    transition: background .4s ease, border-color .4s ease, transform .4s ease;
    position: relative;
    overflow: hidden;
  }
  .option-card:hover {
    background: var(--bone-3);
    transform: translateY(-2px);
  }
  .option-card.selected {
    background: var(--bone-3);
    border-color: var(--gold);
  }
  .option-card .img {
    aspect-ratio: 1 / 1;
    background: linear-gradient(160deg, var(--bone-3) 0%, var(--bone-2) 100%);
    position: relative;
  }
  .option-card .img::after {
    content: "image coming";
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 13px;
    color: var(--charcoal-faint);
  }
  .option-card .check {
    position: absolute;
    top: 14px; right: 14px;
    width: 26px; height: 26px;
    border-radius: 50%;
    background: var(--gold);
    color: var(--bone);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 13px;
    opacity: 0;
    transform: scale(.6);
    transition: opacity .35s ease, transform .35s cubic-bezier(.22,1.5,.36,1);
    z-index: 2;
  }
  .option-card.selected .check {
    opacity: 1;
    transform: scale(1);
  }
  .option-card .body {
    padding: 18px 18px 22px;
  }
  .option-card .label {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 350;
    font-size: 18px;
    color: var(--charcoal);
    margin-bottom: 4px;
  }
  .option-card .note {
    font-family: var(--serif-body);
    font-size: 14px;
    line-height: 1.5;
    color: var(--charcoal-faint);
    font-style: italic;
  }
  .option-card.add-slot {
    border: 1px dashed var(--rule);
    background: transparent;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    min-height: 100%;
  }
  .option-card.add-slot:hover {
    border-color: var(--gold);
    background: rgba(182,139,76,.04);
  }
  .option-card.add-slot .body {
    padding: 40px 20px;
  }
  .option-card.add-slot .label {
    font-size: 15px;
    color: var(--charcoal-faint);
    font-style: italic;
  }

  /* Open text fields */
  .field {
    margin-bottom: 24px;
  }
  .field label {
    display: block;
    font-family: var(--serif-body);
    font-style: italic;
    font-size: 14px;
    color: var(--charcoal-soft);
    margin-bottom: 10px;
    letter-spacing: .04em;
  }
  .field .hint {
    font-size: 13px;
    color: var(--charcoal-faint);
    font-style: italic;
    margin-top: 6px;
  }
  .field input,
  .field textarea {
    width: 100%;
    background: transparent;
    border: 0;
    border-bottom: 1px solid var(--rule);
    padding: 14px 0;
    font-family: var(--serif-body);
    font-size: 17px;
    color: var(--charcoal);
    transition: border-color .4s ease;
    resize: vertical;
  }
  .field textarea {
    min-height: 130px;
  }
  .field input:focus,
  .field textarea:focus {
    outline: none;
    border-bottom-color: var(--gold);
  }
  .field input::placeholder,
  .field textarea::placeholder {
    color: var(--charcoal-faint);
    font-style: italic;
  }

  .open-prompt {
    margin-top: 20px;
    padding-top: 30px;
    border-top: 1px solid var(--rule);
  }
  .open-prompt label {
    display: block;
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 17px;
    color: var(--charcoal-soft);
    margin-bottom: 12px;
    text-align: center;
  }
  .open-prompt textarea {
    background: var(--bone-2);
    border: 0;
    border-bottom: 1px solid var(--rule);
    padding: 16px 18px;
    width: 100%;
    font-family: var(--serif-body);
    font-size: 16px;
    color: var(--charcoal);
    min-height: 90px;
    transition: border-color .4s ease;
  }
  .open-prompt textarea:focus {
    outline: none;
    border-bottom-color: var(--gold);
  }

  .field-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 28px;
  }

  /* Step navigation */
  .step-nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 60px;
  }
  .step-nav .back-btn {
    background: none;
    border: 0;
    font-family: var(--serif-body);
    font-size: 15px;
    color: var(--charcoal-soft);
    letter-spacing: .12em;
    text-transform: uppercase;
    cursor: pointer;
    padding: 16px 0;
    border-bottom: 1px solid transparent;
    transition: border-color .4s ease, color .3s ease;
  }
  .step-nav .back-btn:hover {
    color: var(--charcoal);
    border-color: var(--rule);
  }
  .step-nav .back-btn[disabled] {
    opacity: .35;
    cursor: not-allowed;
  }
  .step-nav .next-btn {
    background: var(--charcoal);
    color: var(--bone);
    border: 0;
    font-family: var(--serif-body);
    font-size: 15px;
    letter-spacing: .14em;
    text-transform: uppercase;
    padding: 18px 44px;
    cursor: pointer;
    transition: background .4s ease, letter-spacing .4s ease, opacity .3s ease;
  }
  .step-nav .next-btn:hover {
    background: var(--gold-deep);
    letter-spacing: .18em;
  }
  .step-nav .next-btn.is-disabled,
  .step-nav .next-btn[disabled] {
    opacity: .35;
    cursor: not-allowed;
    pointer-events: none;
  }

  /* Thank-you */
  .thank-you {
    text-align: center;
    padding: 80px 0;
  }
  .thank-you h2 {
    font-family: var(--serif-display);
    font-weight: 320;
    font-size: clamp(40px, 5vw, 64px);
    margin-bottom: 24px;
    letter-spacing: -.02em;
  }
  .thank-you h2 em { font-style: italic; }
  .thank-you p {
    font-family: var(--serif-display);
    font-style: italic;
    font-weight: 300;
    font-size: 22px;
    color: var(--charcoal-soft);
    max-width: 540px;
    margin: 0 auto 40px;
    line-height: 1.5;
  }
  .thank-you .gold-line {
    width: 0;
    height: 2px;
    background: var(--gold);
    margin: 60px auto 0;
    animation: drawTail 2.5s .5s cubic-bezier(.22,1,.36,1) forwards;
  }
  @keyframes drawTail {
    0%   { width: 0; }
    100% { width: 240px; }
  }

  /* -------- RESPONSIVE -------- */
  @media (max-width: 880px) {
    section { padding: 90px 24px; }
    nav.top { padding: 16px 24px; }
    nav.top.scrolled { padding: 12px 24px; }
    nav.top ul { gap: 22px; }
    nav.top a { font-size: 13px; }
    .hero {
      grid-template-columns: 1fr;
      gap: 50px;
      padding-top: 110px;
      min-height: auto;
    }
    .hero-image { max-width: 420px; margin: 0 auto; }
    .pieces-grid, .gallery-grid, .how-grid { grid-template-columns: 1fr; gap: 40px; }
    .options-grid { grid-template-columns: 1fr 1fr; gap: 14px; }
    .field-row { grid-template-columns: 1fr; gap: 16px; }
    .step-wrap { padding: 0 20px 60px; }
    .modal-top { padding: 20px 24px; }
    footer .container { flex-direction: column; align-items: flex-start; }
    footer .meta { text-align: left; }
    .preview-header { align-items: flex-start; }
    nav.top ul li.nav-link { display: none; }
    nav.top ul { display: none; }
    nav.top .menu-btn { display: flex; }
  }
  @media (max-width: 520px) {
    .options-grid { grid-template-columns: 1fr; }
    .step-question { font-size: 30px; }
  }

</style>
</head>
<body>

<!-- ============== NAV ============== -->
<nav class="top" id="topnav">
  <div class="mark" onclick="scrollTo(0,0)">Solace<span>·</span>Ceramics</div>
  <ul>
    <li class="nav-link"><a href="#story">Story</a></li>
    <li class="nav-link"><a href="#pieces">Pieces</a></li>
    <li class="nav-link"><a href="#how">How it works</a></li>
    <li><a href="#" onclick="openModal('commission'); return false;">Commission</a></li>
    <li><a href="#" onclick="openModal('note'); return false;">A note</a></li>
  </ul>
  <button class="menu-btn" id="menuBtn" aria-label="Open menu" onclick="toggleMobileMenu()">
    <span></span><span></span><span></span>
  </button>
</nav>

<!-- ============== MOBILE MENU ============== -->
<div class="mobile-menu" id="mobileMenu">
  <a href="#story" onclick="closeMobileMenu()">Story</a>
  <a href="#pieces" onclick="closeMobileMenu()">Pieces</a>
  <a href="#how" onclick="closeMobileMenu()">How it works</a>
  <a href="#" onclick="closeMobileMenu(); openModal('commission'); return false;">Commission</a>
  <a href="#" onclick="closeMobileMenu(); openModal('note'); return false;">A note</a>
</div>

<!-- ============== HERO ============== -->
<section class="hero">
  <div class="hero-text">
    <div class="eyebrow"> Hand-thrown</div>
    <h1>Carved by hand. <em><span class="gold-line">Filled in gold.(Or other statement.)</span></em></h1>
    <div class="fill-in" style="margin: 0 0 28px;">
      An introduction. One or two short sentences.
    </div>
    <p class="core-msg">From my grief and healing to hopefully yours (Or other statement.)</p>
    <a href="#story" class="read-link">Read the story →</a>
  </div>
  <div class="hero-image" aria-label="Hero image placeholder"></div>
</section>

<!-- ============== THE SHAPE OF IT ============== -->
<section class="shape">
  <div class="container reveal">
    <div class="divider"></div>
    <div class="fill-in" style="max-width: 640px; margin-left: auto; margin-right: auto;">
      A short paragraph.
    </div>
  </div>
</section>

<!-- ============== A FEW PIECES (PREVIEW) ============== -->
<section class="preview" id="pieces">
  <div class="container">
    <div class="preview-header reveal">
      <div>
        <div class="eyebrow-small">A few pieces</div>
        <h2 class="placeholder-h2">[ A short heading ]</h2>
      </div>
      <a href="#gallery" class="preview-link">See all pieces →</a>
    </div>
    <div class="pieces-grid reveal">
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">Available</span>
      </div>
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">Spoken for</span>
      </div>
      <div class="piece add-piece">
        <div class="piece-image add-piece-img"></div>
        <h3 class="placeholder">+ add a piece</h3>
        <p class="meta">from the editor</p>
      </div>
    </div>
  </div>
</section>

<!-- ============== HOW IT WORKS ============== -->
<section class="how" id="how">
  <div class="container reveal">
    <div class="eyebrow-small">How a piece comes to be</div>
    <h2 class="placeholder-h2">[ A short heading ]</h2>
    <div class="how-grid">
      <div class="how-step">
        <div class="num">i.</div>
        <div class="fill-in compact">
          A sentence or two.
        </div>
      </div>
      <div class="how-step">
        <div class="num">ii.</div>
        <div class="fill-in compact">
          A sentence or two.
        </div>
      </div>
      <div class="how-step">
        <div class="num">iii.</div>
        <div class="fill-in compact">
          A sentence or two.
        </div>
      </div>
    </div>
    <button class="button button-gold" onclick="openModal('commission')">Begin a commission</button>
  </div>
</section>

<!-- ============== THE STORY ============== -->
<section class="story" id="story">
  <div class="read reveal">
    <div class="eyebrow-small" style="text-align:center;">The story</div>
    <h2 style="text-align:center;">From my grief and <em>healing.</em></h2>
    <div style="height: 24px;"></div>
    <div class="fill-in">
      Write here.
      <span class="hint">A paragraph or a few.</span>
    </div>
    <div class="fill-in">
      Write here.
    </div>
  </div>
  <div class="interstitial-image reveal"></div>
  <div class="read reveal">
    <div class="fill-in">
      Write here.
    </div>
    <div class="fill-in">
      Write here.
    </div>
    <div class="fill-in">
      Write here.
    </div>
    <div class="core-message">From my grief and healing to hopefully yours.(Or other statement.)</div>
  </div>
</section>

<!-- ============== FULL GALLERY ============== -->
<section class="gallery" id="gallery">
  <div class="container">
    <div class="reveal" style="text-align:center;">
      <div class="eyebrow-small">The pieces</div>
      <h2 class="placeholder-h2">[ A short heading ]</h2>
      <div class="fill-in" style="max-width: 580px; margin: 0 auto 0;">
        A short paragraph.
        <span class="hint">Optional.</span>
      </div>
    </div>
    <div class="gallery-grid reveal">
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">Available</span>
      </div>
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">In its new home</span>
      </div>
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">Spoken for</span>
      </div>
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">Available</span>
      </div>
      <div class="piece">
        <div class="piece-image"></div>
        <h3 class="placeholder">[ piece title ]</h3>
        <p class="meta">[ form &mdash; bowl, vessel, vase, etc. ]</p>
        <span class="status">One like this can be made</span>
      </div>
      <div class="piece add-piece">
        <div class="piece-image add-piece-img"></div>
        <h3 class="placeholder">+ add a piece</h3>
        <p class="meta">from the editor</p>
      </div>
    </div>
    <div class="center reveal">
      <button class="button" onclick="openModal('commission')">Commission a piece</button>
    </div>
  </div>
</section>

<!-- ============== TAIL / CORE MESSAGE ============== -->
<section class="tail">
  <div class="reveal">
    <p class="core-message"><em>From my grief and healing to hopefully yours.(Or other statement.)</em></p>
    <button class="button" onclick="openModal('note')">Write to me</button>
  </div>
</section>

<!-- ============== FOOTER ============== -->
<footer>
  <div class="container">
    <div class="mark">Solace<span>·</span>Ceramics</div>
    <div class="meta">
      <span class="footer-tagline">[ a short tagline goes here ]</span><br>
      <a href="#" onclick="openModal('note'); return false;">orders@[domain coming soon]</a>
    </div>
  </div>
</footer>


<!-- ============== COMMISSION MODAL ============== -->
<div class="modal" id="modal-commission" role="dialog" aria-modal="true">
  <div class="modal-shell">
    <div class="modal-top">
      <div class="label">Commissioning a <span>·</span> piece</div>
      <button class="close-btn" onclick="closeModal()">Close</button>
    </div>

    <div class="progress" id="progress">
      <div class="dot current"></div>
      <div class="dot"></div>
      <div class="dot"></div>
      <div class="dot"></div>
    </div>

    <div class="step-wrap">
      <!-- STEP 1 -->
      <div class="step active" data-step="1">
        <div class="step-eyebrow">Step one of four</div>
        <h2 class="step-question">What kind of <em>piece</em> are you imagining?</h2>
        <div class="fill-in" style="max-width: 600px; margin: 0 auto 40px;">
          An optional note.
        </div>

        <div class="options-grid" data-step-options="1">
          <button class="option-card" data-value="option-1-a">
            <div class="img"></div>
            <div class="check">✓</div>
            <div class="body">
              <div class="label">[ option label ]</div>
              <div class="note">[ short note ]</div>
            </div>
          </button>
          <button class="option-card" data-value="option-1-b">
            <div class="img"></div>
            <div class="check">✓</div>
            <div class="body">
              <div class="label">[ option label ]</div>
              <div class="note">[ short note ]</div>
            </div>
          </button>
          <button class="option-card" data-value="option-1-c">
            <div class="img"></div>
            <div class="check">✓</div>
            <div class="body">
              <div class="label">[ option label ]</div>
              <div class="note">[ short note ]</div>
            </div>
          </button>
          <div class="option-card add-slot">
            <div class="body">
              <div class="label">+ add option</div>
            </div>
          </div>
        </div>

        <div class="open-prompt">
          <label>Something else? Tell me what you're imagining.</label>
          <textarea data-open="1" placeholder=""></textarea>
        </div>
      </div>

      <!-- STEP 2 -->
      <div class="step" data-step="2">
        <div class="step-eyebrow">Step two of four</div>
        <h2 class="step-question">What <em>size</em> feels right?</h2>
        <div class="fill-in" style="max-width: 600px; margin: 0 auto 40px;">
          An optional note.
        </div>

        <div class="options-grid" data-step-options="2">
          <button class="option-card" data-value="option-2-a">
            <div class="img"></div>
            <div class="check">✓</div>
            <div class="body">
              <div class="label">[ size label ]</div>
              <div class="note">[ short note ]</div>
            </div>
          </button>
          <button class="option-card" data-value="option-2-b">
            <div class="img"></div>
            <div class="check">✓</div>
            <div class="body">
              <div class="label">[ size label ]</div>
              <div class="note">[ short note ]</div>
            </div>
          </button>
          <button class="option-card" data-value="option-2-c">
            <div class="img"></div>
            <div class="check">✓</div>
            <div class="body">
              <div class="label">[ size label ]</div>
              <div class="note">[ short note ]</div>
            </div>
          </button>
          <div class="option-card add-slot">
            <div class="body">
              <div class="label">+ add option</div>
            </div>
          </div>
        </div>

        <div class="open-prompt">
          <label>Something else? Tell me what you're imagining.</label>
          <textarea data-open="2" placeholder=""></textarea>
        </div>
      </div>

      <!-- STEP 3 -->
      <div class="step" data-step="3">
        <div class="step-eyebrow">Step three of four</div>
        <h2 class="step-question">Who is it <em>for?</em></h2>
        <div class="fill-in" style="max-width: 600px; margin: 0 auto 40px;">
          An optional note.
        </div>

        <div class="field">
          <label>Their name (only if you'd like to share it)</label>
          <input type="text" data-field="name-of" placeholder="" />
        </div>

        <div class="field">
          <label>Tell me about them</label>
          <textarea data-field="about" placeholder=""></textarea>
        </div>

        <div class="field">
          <label>A word, phrase, or date you'd like me to keep in mind (optional)</label>
          <input type="text" data-field="word" placeholder="" />
        </div>
      </div>

      <!-- STEP 4 -->
      <div class="step" data-step="4">
        <div class="step-eyebrow">Step four of four</div>
        <h2 class="step-question">How do I <em>reach you?</em></h2>
        <div class="fill-in" style="max-width: 600px; margin: 0 auto 40px;">
          An optional note.
        </div>

        <div class="field-row">
          <div class="field">
            <label>Your name</label>
            <input type="text" data-field="name" placeholder="" required />
          </div>
          <div class="field">
            <label>Email</label>
            <input type="email" data-field="email" placeholder="" required />
          </div>
        </div>
        <div class="field-row">
          <div class="field">
            <label>Phone (optional)</label>
            <input type="tel" data-field="phone" placeholder="" />
          </div>
          <div class="field">
            <label>Mailing address (optional)</label>
            <input type="text" data-field="address" placeholder="" />
          </div>
        </div>
        <div class="field">
          <label>Is there a date this needs to arrive by? (optional)</label>
          <input type="text" data-field="by-when" placeholder="" />
        </div>
      </div>

      <!-- THANK YOU -->
      <div class="step thank-you" data-step="thanks">
        <h2>Thank <em>you.</em></h2>
        <p>Your note has been sent.</p>
        <div class="gold-line"></div>
      </div>

    </div>

    <div class="step-nav" id="step-nav">
      <button class="back-btn" onclick="prevStep()" id="backBtn" disabled>← Back</button>
      <button class="next-btn" onclick="nextStep()" id="nextBtn">Continue →</button>
    </div>
  </div>
</div>


<!-- ============== A NOTE / CONTACT MODAL ============== -->
<div class="modal" id="modal-note" role="dialog" aria-modal="true">
  <div class="modal-shell">
    <div class="modal-top">
      <div class="label">A <span>·</span> note</div>
      <button class="close-btn" onclick="closeModal()">Close</button>
    </div>
    <div class="step-wrap">
      <div class="step active" data-step="note">
        <div class="step-eyebrow">Write to me</div>
        <h2 class="placeholder-h2">[ A short heading ]</h2>
        <div class="fill-in" style="max-width: 540px; margin: 0 auto 40px;">
          An optional note.
        </div>
        <div class="field-row">
          <div class="field">
            <label>Your name</label>
            <input type="text" data-field="note-name" />
          </div>
          <div class="field">
            <label>Email</label>
            <input type="email" data-field="note-email" />
          </div>
        </div>
        <div class="field">
          <label>Your message</label>
          <textarea data-field="note-message" placeholder=""></textarea>
        </div>
        <div class="step-nav" style="justify-content: flex-end;">
          <button class="next-btn" onclick="sendNote()">Send</button>
        </div>
      </div>
      <div class="step thank-you" data-step="note-thanks">
        <h2>Thank <em>you.</em></h2>
        <p>Your message has been sent.</p>
        <div class="gold-line"></div>
      </div>
    </div>
  </div>
</div>


<script>
  // ----- NAV scroll state -----
  const topnav = document.getElementById('topnav');
  window.addEventListener('scroll', () => {
    if (window.scrollY > 40) topnav.classList.add('scrolled');
    else topnav.classList.remove('scrolled');
  });

  // ----- Mobile menu -----
  function toggleMobileMenu() {
    const btn = document.getElementById('menuBtn');
    const menu = document.getElementById('mobileMenu');
    btn.classList.toggle('open');
    menu.classList.toggle('open');
    document.body.style.overflow = menu.classList.contains('open') ? 'hidden' : '';
  }
  function closeMobileMenu() {
    document.getElementById('menuBtn').classList.remove('open');
    document.getElementById('mobileMenu').classList.remove('open');
    document.body.style.overflow = '';
  }

  // ----- Randomize the gold lines on placeholder images so no two look identical -----
  document.querySelectorAll('.piece-image, .story .interstitial-image').forEach(el => {
    const angle = -8 - Math.random() * 28;          // -8 to -36 degrees
    const top = 22 + Math.random() * 30;            // 22% to 52%
    const left = 5 + Math.random() * 14;            // 5% to 19%
    const width = 62 + Math.random() * 26;          // 62% to 88%
    el.style.setProperty('--line-angle', angle + 'deg');
    el.style.setProperty('--line-top', top + '%');
    el.style.setProperty('--line-left', left + '%');
    el.style.setProperty('--line-width', width + '%');
  });

  // ----- Reveal on scroll -----
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
        observer.unobserve(e.target);
      }
    });
  }, { threshold: 0.12 });
  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // ----- Option-card selection -----
  document.querySelectorAll('.options-grid').forEach(grid => {
    grid.addEventListener('click', (e) => {
      const card = e.target.closest('.option-card:not(.add-slot)');
      if (!card) return;
      // single-select per step (toggle off if reclicked)
      const wasSelected = card.classList.contains('selected');
      grid.querySelectorAll('.option-card').forEach(c => c.classList.remove('selected'));
      if (!wasSelected) card.classList.add('selected');
    });
  });

  // ----- Modal logic -----
  let currentStep = 1;
  const totalSteps = 4;

  function openModal(which) {
    if (which === 'commission') {
      document.getElementById('modal-commission').classList.add('open');
      currentStep = 1;
      showStep(1);
    } else if (which === 'note') {
      document.getElementById('modal-note').classList.add('open');
      // reset note modal to first step
      document.querySelectorAll('#modal-note .step').forEach(s => s.classList.remove('active'));
      document.querySelector('#modal-note .step[data-step="note"]').classList.add('active');
    }
    document.body.style.overflow = 'hidden';
  }
  function closeModal() {
    document.querySelectorAll('.modal').forEach(m => m.classList.remove('open'));
    document.body.style.overflow = '';
  }
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeModal();
  });

  function showStep(n) {
    const modal = document.getElementById('modal-commission');
    modal.querySelectorAll('.step').forEach(s => s.classList.remove('active'));
    const target = modal.querySelector(`.step[data-step="${n}"]`);
    if (target) target.classList.add('active');

    // progress dots
    const dots = document.querySelectorAll('#progress .dot');
    dots.forEach((d, i) => {
      d.classList.remove('current', 'done');
      if (i + 1 < n) d.classList.add('done');
      if (i + 1 === n) d.classList.add('current');
    });

    // back / next states
    const backBtn = document.getElementById('backBtn');
    const nextBtn = document.getElementById('nextBtn');
    const stepNav = document.getElementById('step-nav');
    backBtn.disabled = (n === 1);
    if (n === totalSteps) {
      nextBtn.textContent = 'Send →';
    } else {
      nextBtn.textContent = 'Continue →';
    }
    if (n === 'thanks') {
      stepNav.style.display = 'none';
    } else {
      stepNav.style.display = '';
    }

    // re-evaluate validation on entering step 4
    validateNext();

    // scroll modal to top so each step starts clean
    modal.scrollTop = 0;
  }

  function validateNext() {
    const nextBtn = document.getElementById('nextBtn');
    if (currentStep === totalSteps) {
      const name = document.querySelector('#modal-commission [data-field="name"]').value.trim();
      const email = document.querySelector('#modal-commission [data-field="email"]').value.trim();
      const valid = name.length > 0 && /\S+@\S+\.\S+/.test(email);
      nextBtn.disabled = !valid;
      nextBtn.classList.toggle('is-disabled', !valid);
    } else {
      nextBtn.disabled = false;
      nextBtn.classList.remove('is-disabled');
    }
  }

  // wire up live validation on the required fields
  document.querySelectorAll('#modal-commission [data-field="name"], #modal-commission [data-field="email"]').forEach(el => {
    el.addEventListener('input', validateNext);
  });

  function nextStep() {
    if (currentStep < totalSteps) {
      currentStep++;
      showStep(currentStep);
    } else {
      // validate then submit
      const nameField = document.querySelector('#modal-commission [data-field="name"]');
      const emailField = document.querySelector('#modal-commission [data-field="email"]');
      if (!nameField.value.trim() || !/\S+@\S+\.\S+/.test(emailField.value.trim())) {
        if (!nameField.value.trim()) nameField.focus();
        else emailField.focus();
        return;
      }
      submitCommission();
    }
  }
  function prevStep() {
    if (currentStep > 1) {
      currentStep--;
      showStep(currentStep);
    }
  }

  function gatherCommissionData() {
    const data = {};
    // selected options per step
    document.querySelectorAll('.options-grid').forEach(grid => {
      const stepNum = grid.getAttribute('data-step-options');
      const sel = grid.querySelector('.option-card.selected');
      data[`step_${stepNum}_selection`] = sel ? sel.getAttribute('data-value') : null;
    });
    // open prompts
    document.querySelectorAll('[data-open]').forEach(t => {
      data[`step_${t.getAttribute('data-open')}_open`] = t.value;
    });
    // step 3 + 4 fields
    document.querySelectorAll('#modal-commission [data-field]').forEach(f => {
      data[f.getAttribute('data-field')] = f.value;
    });
    return data;
  }

  function submitCommission() {
    const data = gatherCommissionData();
    // In production: send to founder's email via form service (Squarespace forms, Jotform, Formspree, etc.)
    console.log('Commission request to be emailed:', data);

    // Show thank-you screen
    const modal = document.getElementById('modal-commission');
    modal.querySelectorAll('.step').forEach(s => s.classList.remove('active'));
    modal.querySelector('.step[data-step="thanks"]').classList.add('active');
    document.getElementById('step-nav').style.display = 'none';
    // turn all dots gold-done
    document.querySelectorAll('#progress .dot').forEach(d => {
      d.classList.remove('current');
      d.classList.add('done');
    });
    modal.scrollTop = 0;
  }

  function sendNote() {
    const data = {};
    document.querySelectorAll('#modal-note [data-field]').forEach(f => {
      data[f.getAttribute('data-field')] = f.value;
    });
    console.log('Note to be emailed:', data);
    const modal = document.getElementById('modal-note');
    modal.querySelectorAll('.step').forEach(s => s.classList.remove('active'));
    modal.querySelector('.step[data-step="note-thanks"]').classList.add('active');
    modal.scrollTop = 0;
  }
</script>

</body>
</html>
